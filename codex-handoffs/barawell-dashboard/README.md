# Barawell Dashboard handoff

This branch is prepared for continued editing in Codex.

Start with `AGENTS.md`. The original dashboard was supplied in the ChatGPT conversation as `Barawell Dashboard.dc.html` and is cited in the handoff conversation. Two temporary transfer chunks are present only as transport artifacts; do not integrate them directly into the application. Reconstruct or re-import the original source before implementation, then migrate it into maintainable components as described in `AGENTS.md`.

Recommended first milestone: implement the responsive dashboard shell and Overview page without changing existing production integrations.
