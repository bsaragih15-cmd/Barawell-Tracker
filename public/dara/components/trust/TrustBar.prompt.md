Quiet trust strip (hairline top/bottom, dot separators) — under heroes and above footers.

```jsx
<TrustBar />
<TrustBar items={["Obat terdaftar BPOM", "Dokter berlisensi"]} />
```

Text only, no badges/shields — restraint is the trust signal.
