Consultation status pill — colors are intent (sage = progress/approved, info blue = action needed).

```jsx
<StatusChip status="in_review" />
<StatusChip status="approved" />
<StatusChip status="needs_info" />
```

Never red for a rejection — `rejected` renders neutral; the kindness lives in the surrounding copy.
