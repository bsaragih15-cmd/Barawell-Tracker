Form field with label, hint, unit suffix, and inline word-based errors (WCAG: never color alone).

```jsx
<Input label="Berat badan" type="number" suffix="kg" hint="Perkiraan saja tidak apa-apa" />
<Input label="Email" error="Format email belum benar" />
```

16px input text (prevents iOS zoom), 48px min height, terracotta focus ring.
